services:
  cattermeet:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data/uploads:/app/uploads
      - ./data/db:/app/db
    environment:
      - DB_PATH=/app/db/cattermeet.db
      - UPLOAD_DIR=/app/uploads
    restart: unless-stopped
